  <style>
  *{
    margin: 0;
    padding: 0;
    font-family: Arial, Helvetica, sans-serif;
    }

    body{
        background-color: #FF5733
    }

    header{
        text-align: center;
        padding: 50px 0;
        font-size: 25px;
    }

    img{
        height: 500px;
        display: flex;
        margin-left: auto;
        margin-right: auto;
    }

    P{
       text-align: center;
       color: #fff;
    }

        input{
        background-color: #0000ff;
        font-size: 25px;
    }
  </style>

  {{>header}}

      <div class="container p-3 my-3 " >
      <div class="d-flex justify-content-center m-2">
          <form action="/book/{{book.ISBN}}/drop" method="POST">

    <img src={{book.imageUrl}} alt="Book"><br>
    <p>{{book.ISBN}}</p>
    <p>{{book.author}}</p>
    <p>{{book.title}}</p>
    <p>{{book.language}}</p>
    <p>{{book.pages}}</p>
    <p>{{book.genre}}</p>
    <p>{{book.storage}}</p>
    <p>{{book.releseDate}}</p>
     <p>{{book.publisher}}</p><br><br>
    <p>Are you sure you want to delete this book?<p><br>


          <a href="/book/{{book.ISBN}}/edit" class="btn btn-primary btn-sm">Edit</a><!--Sends data to action-->

          <a href="/book/{{book.ISBN}}/drop" class="btn btn-primary btn-sm">Delete</a><!--Sends data to action-->

      </form>
      </div>

  </div>
  </div>
  </div>
{{>footer}}